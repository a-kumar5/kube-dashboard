replicaCount: 1

namespace: dev
serviceName: kube-timed-scheduler
selectorLabels: kube-timed-scheduler

image:
  repository: connectayushkumar/kube-timed-scheduler
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: latest

service:
  port: 80
  targetport: 8080